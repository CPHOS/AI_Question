"""
任务执行编排包（后端复用）。

本包封装一次生成任务从「绑定统计上下文 → 运行状态机 → 采集 API 额度 →
写盘产物」的完整流程，供 :mod:`api` 后端调用：

- :mod:`app.runner`：统一的任务执行入口（:func:`app.runner.execute_task`）。
- :mod:`app.outputs`：产物写盘、LaTeX 编译与 API 额度查询。

本系统为纯后端服务，所有任务均通过 FastAPI 接口（见 :mod:`api`）提交与调度；
不再提供命令行入口。所有审核（数学 / 物理 / 结构 / 质量 / 仲裁）均在状态机内
由本仓库的 Agent 完成，不集成任何外部审题工具。
"""
